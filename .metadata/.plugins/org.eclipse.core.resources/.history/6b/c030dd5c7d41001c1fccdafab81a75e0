#include <stdio.h>

int main () {

	int codigo, vetor[5];

	printf("Digite o código: ");
	fflush(stdout);
	scanf("%d", &codigo);

	if (codigo > 0) {
		if (codigo == 1) {
			for (int i = 0; i < 5; i++) {
				printf("Digite um número: ");
				fflush(stdout);
				scanf("%d", &vetor[i]);
			}
		}
	}
}
