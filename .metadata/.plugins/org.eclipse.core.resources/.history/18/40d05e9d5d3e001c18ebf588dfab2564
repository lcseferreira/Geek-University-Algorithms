#include <stdio.h>

int main() {
	int num, a, b;

	printf("Digite um número: ");
	fflush(stdout);
	scanf("%d", &num);

	if (num > 0) {
		a = num;
		printf("Valor positivo")
		printf("%d", a);
	} else {
		num = 0;
		printf("%d", num);
	}
}
