#include <stdio.h>
#include <ctype.h>

int main() {
	float peso, excesso, multa;

	printf("Digite o peso dos peixes: ");
	fflush(stdout);
	scanf("%f", &peso);

	if (peso > 50) {
		excesso = peso - 50;
		multa = excesso * 4;
		printf("O valor da multa é de R$%.2f");
	}
