#include <stdio.h>

int main() {
	int id, defeito, quantidade, necessita_esfera, necessita_limpeza, necessita_cabo, quebrado;

	printf("Digite o id do mouse: ");
	fflush(stdout);
	scanf("%d", &id);

	while (defeito != 0) {

	}
}
